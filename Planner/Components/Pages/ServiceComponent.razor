<MudStack Class="p-1">
    <MudPaper Class="p-1" Outlined="true">
        <MudText>Услуга: <b>@Service.Name</b></MudText>
        <MudText>План: <b>@Service.Plan</b> шт</MudText>
        <MudGrid Spacing="0">
            <div class="wrapper_week_plan">
                <MudText> Факт, шт:</MudText>
                <MudNumericField @bind-Value="@Service.Fact" @ref="StringFactRef" OnBlur="@(async()=> await UpdateCompanyAsync(Service.Name))" DisableUnderLine="true"
                                 Adornment="Adornment.None" AdornmentText="шт" T="double?" HideSpinButtons="true"
                                 Validation="@(new Func<double?, IEnumerable<string>>(NumberStrength))" Immediate="true" MaxLength="10"
                                 Converter="@_converter"
                                 Style="height:14px; width:120px; font-weight:bold; margin:0px; margin-left:3px" />
                <MudTooltip>
                    <MudIconButton OnClick="@(() => OpenNumericDialogAsync())" Icon="@Icons.Material.Outlined.Add" Color="Color.Success" Size="Size.Small" />
                    <MudIconButton OnClick="@(() => StringFactRef.FocusAsync())" Icon="@Icons.Material.Outlined.Edit" Color="Color.Success" Size="Size.Small" />
                </MudTooltip>
            </div>
        </MudGrid>
        <MudText>Факт на текущую дату: <b style="color: @ColorCurrentExecutionPercentage">@CurrentExecutionPercentage</b> %</MudText>
        <MudText>Разница на текущую дату: <b style="color: @ColorDeltaFact">@DeltaFact</b> шт</MudText>
    </MudPaper>
    <MudPaper Class="p-1" Outlined="true">
        <MudText>До 85%: <b style="color: @ColorEightyFive">@EightyFive</b> шт</MudText>
        <MudText>До 100%: <b style="color: @ColorOneHundred">@OneHundred</b> шт</MudText>
    </MudPaper>
    <MudPaper Class="p-1" Outlined="true">
        <MudText>Целевой <b>%</b> на текущую дату: <b>@TargetPercentage</b>%</MudText>
        <MudText>Факт целевого <b>%</b> на текущую дату: <b style="color: @ColorCurrentDatePercentage">@CurrentDatePercentage</b>%</MudText>
    </MudPaper>
</MudStack>





